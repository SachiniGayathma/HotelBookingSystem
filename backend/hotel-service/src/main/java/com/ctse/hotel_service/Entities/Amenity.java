package com.ctse.hotel_service.Entities;

//Amenity is an enum that represents the various amenities that a hotel can offer.
public enum Amenity {
    WIFI,
    POOL,
    PARKING,
    RESTAURANT,
    GYM,
    SPA
}
